# Решения — указатель

> Здесь только указатель. Полный текст решения с причиной — в `docs/decisions/YYYY-MM-DD-<slug>.md`
> (образец формата — `2026-08-22-navigaciya-i-gamma.md`: решение · почему · цена ошибки · что не отменяется).
> Правило: решение без записанной причины пересматривается дёшево и вслепую — так больше не делаем.

| Дата | Решение | Где |
|---|---|---|
| 2026-08-22 | Р-1 «Номенклатура» — отдельный сквозной разрез VendHub (ключ `settings` сохранён); Р-2 подписи вкладок русские | `docs/decisions/2026-08-22-navigaciya-i-gamma.md` |
| 2026-08-24 | Мутирующие формы CC: `onSubmit` + server action в `startTransition`, поля сохраняют ввод при ошибке | `CLAUDE.md` → Правила разработки; эталон `apps/cc/src/components/customs-rates.tsx` |
| 2026-08-19 | Прямой пуш в main запрещён (прод-деплой без CI-гейта) | `.claude/hookify.block-push-to-main.local.md` |
| 2026-08-04 | GLOBERENT: переносится вся ERP PROMACH, модуль за модулем | `docs/PROMACH_TRANSFER.md` |
| 2026-07-29 | Оранжевый — лицо продукта: «увидел оранжевое — надо нажать»; две глубины (`--accent` заливка, `--hot` тонкое) | `apps/cc/src/app/globals.css` (комментарии к токенам) |
| 2026-09-04 | MYDON строится по ARMS (Skills → Memory → Routines → Applications); память — роутер-файлы; факты — Core, знания — markdown | `docs/AGENTIC_OS_ARMS_PLAN.md` |
| 2026-09-04 | Markdown-навык без кода исполняется общим `executor: llm` (один metered-вызов, строгий JSON → Proposal); агенты — только metered `openai-api`, не Agent SDK/подписка; первый паспорт `globerent-sales/qualify-lead`; ответ не по контракту блокирует задачу (`skill_failed`) до owner retry | `docs/superpowers/specs/2026-09-04-llm-skill-executor-design.md` |
| 2026-09-04 | Данные, заполненные Claude (паспорта, KB, цели), — значения по умолчанию до ревизии владельца; ревизия идёт квизом по одной карточке, система их не ждёт | `docs/FIRST_LOGIN_CHECKLIST.md`, `docs/superpowers/specs/2026-09-04-owner-review-quiz-design.md` |
| 2026-09-04 | Узлы VendHub (ответы владельца, спека §11): номера `M-`/`G-`/`B-`/`H-<набор>-<поз>`/`F-` выдаёт система при заведении, сотрудники наклеивают и подтверждают; бункеров **всегда 8**; сушка после мойки (`PARTS_DRYING_STAGE=1`); фото при инвентаризации обязательно, пропуск по причине (`PARTS_COUNT_PHOTO_REQUIRED=1`); товары (У6) — после узлов, через складской леджер, флип `VENDING_STOCK_SOURCE=ledger` после недели нулевой сверки; списание заливкой включено, но только по точным данным | `docs/superpowers/specs/2026-09-04-vendhub-parts-inventory-design.md` §11–12, `docs/OWNER_NEXT_STEPS_parts.md` |
| 2026-09-05 | Каталог навыков — зеркало файлов в `agent_skill_catalog` (агенты переписывают целиком на старте); запуск из deck — обычная задача агенту (`source=skills-deck`), пауза уважается сразу; per-run настраивается только усилие; llm-навык на cron — только durable-задачей; явный навык задачи побеждает подбор по заголовку | `docs/decisions/2026-09-05-skills-deck-cron-llm.md` |
| 2026-09-05 | Venture Factory: навык перенесён из восстановленного чата дословно; референсы канонично в KB агентов (`owner-profile.md` — только в навыке, least privilege); реестр находок — `entity(type='venture_candidate')` в домене `mydon` до появления `ventures`; файл сессии — формат авторства и сид, импорт `tools/import-ventures.mjs`; `new_direction.py` остаётся как есть до фазы Builder | `docs/decisions/2026-09-05-venture-factory-interactive.md` |
| 2026-09-06 | Волна M: зеркала движка под тестом дрейфа, не генерируются; документы отдаёт Core с диска образа за сервисным токеном (личные корни — за owner-токеном); граф строит Core, включая `mentions` по путям в бэктиках; `marked` экранирует сырой HTML (XSS найден и закрыт в ревью); `/brain` — `d3-force` на canvas; правка документов и RAG — не в этом срезе; `PROTOCOL.md`/`MYDON_AGENT_BUILDER.md` не найдены нигде — заменены `_template/` + `direction-package.md` + восстановленный дословно `run-log.md` | `docs/decisions/2026-09-06-wave-m-docs-brain.md` |
| 2026-09-06 | Волна R: журнал прогонов `agent_run` пишет рантайм (идемпотентно по `request_key`, никогда не блокирует навык); снимок расписаний пушит рантайм, `nextRun` считает Core, снимок старше 15 мин — предупреждение; шесть env-мониторов — те же задания на доске и в журнале (`agent_name="system"`); хуки паспорта декларативны (`source_fresh`, `quiet_hours`, `coach_lite`), неизвестный `pre_run` блокирует навык; плейбэк `/flows` собирается из имеющегося по `agent_run`; паузы на доске — те же два ключа `system_config`, per-job паузы нет; `/repo-audit` — скрипт в `tools/` + навык Claude Code, расписание за владельцем; причины пропуска — один словарь `RUN_SKIP_REASONS` | `docs/decisions/2026-09-06-wave-r-crons-flows.md` |
| 2026-09-06 | Волна A1: MCP-сервер `mydon-core` и CLI `mydon` — две оболочки над одним клиентом Core (`apps/mcp`, третья копия fetch не заводится); инструменты `<область>_<действие>`, читающие отдельно от меняющих (19 = 13 + 6), первое предложение описания говорит, что изменится и где видно; пояс владельца — правда в описании `approval_decide` по фактическому состоянию Core; личный контур исключён явно, не гардом; ответы — компактный текст, не сырой JSON; сухой прогон CLI — одна конвенция `--yes`; ошибки Core переводятся, не пробрасываются | `docs/decisions/2026-09-06-wave-a1-mcp-cli.md` |
| 2026-09-06 | Волна A2 «Лица»: занятость агента считает Core (`GET /agents/status`: working/blocked/paused/idle по живому claim, не по журналу); системная пауза (`AGENTS_TASKS_PAUSED`/`AGENTS_SCHEDULES_PAUSED`) перекрывает всё и называет себя; «Приложения» делят источники по природе связи (снаружи vs внутренние мониторы); три состояния здоровья, «не оценить» не похоже на «в порядке»; heartbeat бота — событие, не таблица; агрегат здоровья одним маршрутом Core; карточка агента показывает то, что есть; ни одной миграции | `docs/decisions/2026-09-06-wave-a2-faces.md` |
| 2026-09-07 | Срез A3 «Кольцо артефактов»: премиса плана («данные уже в Core — нужна витрина») не подтвердилась — `document` мёртвая, бот терял документы; субстрат — `attachment` (+`title`/`domain`/`tags`, миграция 0089, индекс обычный — мигратор держит одну транзакцию), `document` помечена, не снесена; владелец чат-документа — `person` (`createdBy = person:<uuid>`), гость не сохраняется; бот сохраняет ДО отправки, сбой хранилища не блокирует отправку и назван ярлыком; `GET /artifacts` за `ReadTokenGuard`, без содержимого и `storageKey`; витрина тёмная строкой в `CONSOLE_ROUTES` (Д2 смержен, `ConsoleTheme` снесён); ловушка §6 п. 3 подтвердилась — `attachment` закрыт токеном ЦЕЛИКОМ (`raw` отдавал файл по id без проверки, а `GET /attachments` — пресайнед-ссылку S3 в обход `raw`), панель носит токен и в прокси; критерий плана переформулирован — только артефакты после `ARTIFACTS_SINCE`; КП/договоры, coach-diff, полнотекст — не в срезе; долг A2 по `agent_run` в 0089 не вошёл | `docs/decisions/2026-09-07-artifacts-ring.md` |

Новое решение: добавь строку сюда и файл в `docs/decisions/`. Не дублируй текст.
